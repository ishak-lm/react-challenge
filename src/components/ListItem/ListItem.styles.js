import styled from 'styled-components'

const Li = styled.li`
  height: 25px;
  display: flex;
  justify-content: space-around;
  position: relative;
  align-items: center;
`

const Span = styled.span`
  z-index: 1;
`

export default {
  Li,
  Span,
}
